# lwt.web
Web ui for lwt

# getting started
* Clone the repo
* Run `yarn install`
* Run `yarn start`

# use custom BE
```shell
export REACT_APP_API_ROOT=<your BE url>
```

# try demo here/
https://lwt.hai.fyi
